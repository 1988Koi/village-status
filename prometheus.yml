global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "Village_App"
    metrics_path: /metrics
    static_configs:
    - targets: ["village-app:5000"]

rule_files:
  - "alert.rules.yml"

alerting:
  alertmanagers:
    - static_configs:
      - targets: ["alertmanager:9093"]